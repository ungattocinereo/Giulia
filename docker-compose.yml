services:
  popova-website:
    build: .
    container_name: popova-website
    ports:
      - "3001:80"
    restart: unless-stopped
    volumes:
      # Mount images directory for easy updates without rebuild
      - ./public/images:/usr/share/nginx/html/images:ro
    environment:
      - NODE_ENV=production
